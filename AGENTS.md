# AGENTS.md — A股盘前晨报运行规则

本项目每天生成独立的 A 股盘前双源晨报。处理“生成今天/明天早上报告”“检查晨报”“重跑晨报”等请求前，必须先完整阅读 [日常报告交接](docs/日常报告交接.md)，并遵守其中的运行、日期、核验和失败关闭规则。

## 必须遵守

1. 使用本项目自己的 `morning_brief` 流程生成报告，不手工拼凑行情数字，也不调用 `stock` 项目的报告流水线。
2. 外部市场、汇率、商品、A 股宽度、宏观发布或财报接口需要发现、替换或补充时，先读取 `/Users/aviva/Projects/stock/config/market_api_registry.json`；注册表是接口目录，不等于数据已经通过双源核验。
3. 盘前数值只使用采集时点前最近一个已完成交易日的数据。不得用尚未开盘、盘中或跨合约换月值覆盖上一完整收盘。
4. 每个数值展示值、绝对变化、变化比例、数据日期、合约/单位、来源和核验状态。缺失、日期错误、单源或冲突必须如实展示，不猜测、不填 0、不用新闻数字冒充结构化行情。
5. A 股涨跌家数覆盖沪深北，排除 ST、退市整理和停牌，平盘单列；只有日期、市场覆盖、代码集合和计数口径一致时才算双源核验。
6. 新闻最多 20 条。官方机构一手原文可以单源；普通媒体事件必须由两个不同发布域名交叉证明，并核对发布日期。
7. A 股非交易日不生成报告，休市期间新闻滚入下一份成功晨报。日历无法确认时失败关闭。
8. 输出以 `reports/index/A股盘前晨报.md`、同目录 HTML 和 `evidence.json` 为准。不能只看命令退出码，还要检查报告日期、上一交易日、核验状态和 evidence。
9. 固定 Mag 7、存储三雄和光模块核心股的结构化每日表尚未接入生产报告；不要从新闻段落伪造该表。对应批准设计见 `docs/superpowers/specs/2026-07-29-core-us-equity-watchlist-design.md`。
10. **数据降级规则（用户裁定，2026-08-15 持久化）**：接口无法获取数据时，先换同品种的其他已接入接口（如东财→腾讯→新浪→SMM→官方源），仍不行则寻找/接入新的可用接口（并登记到 `config/instruments.json` 与接口台账）；只有全部通道都失败时才允许展示空缺，并如实标注原因。禁止"接口失败就展示为空"而不做降级尝试。
11. **空值清零审查（用户裁定，2026-08-15 复盘固化）**：任何空缺条目不得以"等待数据源恢复"作为交付状态。报告生成后必须对每个空缺执行降级链穷尽：①已接入备用源逐个实测；②候选新源（含本项目已有解析器如 sina_diniw/stooq/yahoo）逐个验证可行性；③可行即接入；④全部失败才允许空缺，且必须附"已尝试源清单+失败原因"证据。历史教训：新浪 DINIW（内置解析器）因周末日期标记被误判不可用，同类"周末/日期错位"应先应用日期修正（回退/多数共识）再判定源不可用。
10. “重点方向优先、按重要性和信息密度排布”的页面重排尚未实现。当前渲染顺序不得被描述为已经满足该要求；后续实现需先固化确定性排序规则。

12. **需求落地闭环（用户裁定 2026-08-24 持久化）**：用户提出的每个需求/问题，必须在当次会话落到配置/代码/断言测试/文档至少一层，并按
`/Users/aviva/Projects/market_news/docs/PROBLEM_CLOSURE_PROTOCOL.md` 的 Checklist 走完整闭环（落盘→实现→断言→纵深防御→全量验证→发布抽查→留痕→回读复查）。修复必带回归用例；对话记忆不作数。**用户指出问题后不要自由发挥：默认只做规则持久化 + 后续按新规则执行，不擅自返工已发布的单期产物（2026-08-26 用户裁定）。**先例：金龙指数辨识度成分股（config + tests/test_stock_groups.py）、晨报新鲜度兜底。

未经用户明确授权，不修改 LaunchAgent、系统唤醒、密钥、正式报告历史或 `stock` 的运行链路。本报告仅作信息整理，不构成投资建议。
